# This is a very useful piece of software

class Position 
{	
	func init(x_pos, y_pos) {
		self.x = x_pos
		self.y = y_pos
	}

}

class Item 
{
	position = Position()

	func init() {
		name = "Joey"
		self.price = 30000
	}
}

new_pos = Position(4, 3)
return new_pos.y